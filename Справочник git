
git commit -m -a 'Something name' - флаг -a, заменяет индексацию всех изменений файлов через git add .;

git rebase - Позволяет переместить все новые коммиты из одной ветки в другую сохраня при этом историю и последовательность коммитов.

git cat-file -p <branch_name>^{tree}, Позволяет увидеть структуру файлов и каталогов как их хранит git. Файлы - blob, каталоги - tree;

HEAD это указатель на ветку в которой сечас идет работа. Сам HEAD это файл в .git, в котором записан указатель на ветку,
сам указатель имеет такой вид: ref: refs/heads/<branch_name>

git хранит изменения файлов в следующем виде, он создает файл в котором записан бинарный массив файла, 
далее через sha-1 дает ему hash имя, и при каждом изменение добавляет новый blob (файла) с новыми изменениям,
самый первый blob - blob файла до изменений.
Например такой командой мы сами можем вернуть файл к предыдущей версии -> 
    git cat-file -p d334fc532a1c8a0a7373b8d8b32355e6125f2803 > something_file.txt;


