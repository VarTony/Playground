#include <stdio.h>

int main (int argc, char* argv[]) {

	printf("Hello, %s!\n", "C");
	return 0; // Код ошибки(0 - нулевой код ошибки, то есть ошибки нет. PS коды ошибок и их опиписание нужно составлять самому);


}